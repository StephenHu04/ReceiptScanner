name: receipt_scanner
description: A receipt scanning and expense tracking application.
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.0.1
  image_picker: ^1.0.4
  http: ^0.13.6
  mime: ^1.0.4
  http_parser: ^4.0.2
  cupertino_icons: ^1.0.2
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true 